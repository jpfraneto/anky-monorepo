{
  "name": "anky",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:api": "bun run --filter @anky/api dev",
    "dev:web": "bun run --filter @anky/web dev",
    "build:web": "bun run --filter @anky/web build",
    "build:deploy": "bun run --filter @anky/web build",
    "db:generate": "bun run --filter @anky/api db:generate",
    "db:push": "bun run --filter @anky/api db:push",
    "db:studio": "bun run --filter @anky/api db:studio"
  }
}

oh yeah. but the prompt im being displayed is that ugly written on top of it. so it looks very weird. when the image is generated and that overaly with the prompt is added. save a version of the image that doesn't have that overlay. and serve that one as the background one.

and also, another feature / idea that i just thought of. on the moment of a user writing an anky, we do a processing of it via a simple llm, that adds proper punctuation, spacing, and that makes the text have good reading formatting so that if anyone comes and reads that anky it is properly written for  the mind to be able to read it. i was just reading the last anky i wrote and it is a mess. so the ui could be cool because we can show the raw anky that the user wrote without ai filtering and processing to make it nicer so that  with a dark purple color and then on top of that the properly formatted one. and if the user taps the purple one anywhere they get the horrible one more visible than the white one. and viceversa. actually if they tap or click the screen. its very simple.
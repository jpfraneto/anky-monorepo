Cache mirror results by FID so repeat lookups are instant (21ms vs 35s). Add POST /api/mirror/chat endpoint that lets you talk to a mirror's anky — the anky speaks from the mirror context using the public_mirror, bio, and flux_descriptors as conversational memory. Multi-turn chat with history. Frontend: chat input below mirror result and gallery detail views, regenerate button to force a fresh mirror.
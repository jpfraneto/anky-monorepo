now we need to iterate on how this is displayed when the user sends a message that is shorter than 8 minutes. right now i see a weird loading state that is letters going up and down and then i see the response from anky and then i see the reflection and a "write again" button. that looks odd. if the user keeps writing they keep writing. its just that. and once the conversation "started" we just need to display the user a chat interface. a simple chat interface
some things that need to be fixed: after i write my anky, as the writing from anky is being streamed into the page, the layout jumps all around. that's very weird ux. the only way the page can move is because the user moves it. not because the streaming of the text made it bigger and the layout is adjusting. please. that's very important. and also, those buttons that appear after the streaming ends with the three options? those options don't need to be hardcoded. they need to be generated by anky when the reflection is being generated also. they need to correspond to the reflection that was generated. not hardcoded. not always the same. that is the whole point. and the style of those buttons? its very weird. it is not picking up any css. its just a raw html button. please fix that. make this design work properly
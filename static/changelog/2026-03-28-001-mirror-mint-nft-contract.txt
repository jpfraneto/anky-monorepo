Move tagline to bottom of miniapp landing, restyle CTA buttons to match ankycoin.com website design (gold Cinzel serif with angled clip-path). Add mint gate before sharing: "mint anky - 1 usdc" button, share disabled until minted. Write AnkyMirrors.sol ERC-721 smart contract: 4444 supply, 1 USDC each, 1 per FID, EIP-712 signature from backend signer. Backend: POST /api/mirror/mint-sig generates EIP-712 signature for mint, GET /api/mirror/metadata/{id} serves ERC-721 metadata JSON with traits.
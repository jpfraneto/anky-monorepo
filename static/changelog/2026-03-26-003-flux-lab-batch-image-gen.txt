i need to come up with a flow for creating images. something that works very fast and that uses the local flux of comfy ui that we have here to generate images from an array of prompts. so there is an interface that is served here that has a textarea that accepts an array of prompts and then from there it takes the prompts and tells me how many of them there are and then it tells me to hit a button and that button sends all those prompts for them to be processed and the images generated and stored on the flux folder under a experiment-N where N is the numeral for that spcecific set of prompts. and then there is a tab on the left where i can click on each one of the experriments and see all the images that have been generated as a conseqeuncxe of them
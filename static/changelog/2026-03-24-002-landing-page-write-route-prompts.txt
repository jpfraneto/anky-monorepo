epic. i just rewrote the landing page. please implement this one (full HTML provided). and on /write we can add prompts. and when a user creates a prompt that they want to send to other user they just go to /prompt and fill the textarea. now we generated it using flux so it is free for the user. and they get the prompt right away. the link is the link https://anky.app/write?prompt=${encoded_uri}
Evolve the mirror pipeline to generate much more distinctive Anky images. Claude now returns 4 new visual descriptors per user: held_object (symbolic item derived from their casts), background_scene (environment from their world), clothing_detail (unique garment/armor), symbolic_marking (body markings beyond the default). The ComfyUI prompt weaves all of these in so each Anky actually looks different. Also: /image.png on ankycoin.com now dynamically serves the latest mirror with the user's PFP composited as a circular overlay in the bottom-right corner (gold border, server-side image compositing via the image crate). Farcaster frame image auto-updates.